// Documentation for Enum
// The SortDirection enum is used to define the possible directions
// for sorting operations. It includes two possible values:
// ASC: Represents an ascending sort order.
// DESC: Represents a descending sort order.
export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc',
}