/**
 * Enum representing type preferences with their corresponding integer values.
 * 
 * Values:
 * - PREFERENCE: Indicates a preference with a value of 1.
 * - EXCLUSION: Indicates an exclusion with a value of 2.
 */
export enum TypePreference {
  PREFERENCE = 1,
  EXCLUSION = 2,
}